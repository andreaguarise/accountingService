<%= form_for(@database_descr) do |f| %>
  <% if @database_descr.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@database_descr.errors.count, "error") %> prohibited this database_descr from being saved:</h2>

      <ul>
      <% @database_descr.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :backend %><br />
    <%= f.text_field :backend %>
  </div>
  <div class="field">
    <%= f.label :version %><br />
    <%= f.text_field :version %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
