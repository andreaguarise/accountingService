<H3>Database Records</H3>

<div id = "sidemenu">
<ul>
<li><%= link_to 'Record list', database_records_url%></li>
<li><%= link_to 'Record stats', stats_database_records_path%></li>
</ul>
</div>

<div id = "sidesearch">
<%= form_tag(search_database_records_path, :method => 'get' ) do %>
  <% DatabaseRecord.attrSearchable.each do |item| %>
	<div class="field">
    	<p><%= item %>:</p>
    	<%= search_field_tag (item, params[item]) %>
  	</div>
  <% end %>
  <div>
  <p></p>
  <%= submit_tag("Search...") %>
  </div>
<% end %>
</div>