<%= form_for(@benchmark_type) do |f| %>
  <% if @benchmark_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@benchmark_type.errors.count, "error") %> prohibited this benchmark_type from being saved:</h2>

      <ul>
      <% @benchmark_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
